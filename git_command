git status;
git add .
git commit -am "初次提交";
git push -u develop master //”develop” 是刚刚创建的远程仓库名，“master”是本地仓库的主干分支（目前也只有这一个分支，没有其他分支）
git remote -v

git log //查看提交历史
git reset --hard commitId  //重置到某次提交 


No refs in common and none specified; doing nothing.
//如ssss果出现以上提示,更新，提交，需使用如下命令
git push origin master
git pull origin master 
